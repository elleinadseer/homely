const User = require('./User')
const Property = require('./Property')
const PropertyType = require('./PropertyType')
const Image = require('./Image')

module.exports = { User, Property, PropertyType, Image }
